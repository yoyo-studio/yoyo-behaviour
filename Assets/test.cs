using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using YoYoStudio.Behaviour;
public class test : YoYoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
